# Nixpacks/Buildpack 빌더를 쓰는 호스트용 시작 명령 (Render, Heroku 등).
#
# 주의: Railway는 railway.toml에서 Dockerfile 빌더를 쓰므로 이 파일을 읽지 않는다.
# Railway의 마이그레이션은 railway.toml의 preDeployCommand가 담당한다.
release: python scripts/migrate.py
web: uvicorn app.main:app --host 0.0.0.0 --port $PORT
